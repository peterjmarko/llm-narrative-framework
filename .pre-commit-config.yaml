repos:
  - repo: local
    hooks:
      - id: build-docs
        name: Build documentation from template
        entry: python -m pdm run python src/build_docs.py
        language: system
        pass_filenames: false
        files: ^(README\.template\.md|docs/diagrams/.*)$
        always_run: true
        verbose: true